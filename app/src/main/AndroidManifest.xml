<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.shimmy.toolsmanagement">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".BarcodeScannerActivity"></activity>
        <activity android:name=".AddToolActivity" />
        <activity android:name=".AlreadyUsedToolsActivity" />
        <activity android:name=".FavoriteActivity" />
        <activity android:name=".WishlistActivity" />
        <activity android:name=".CurrentlyTakenToolsActivity" />
        <activity android:name=".SignUpActivity" />
        <activity
            android:name=".CaptureAct"
            android:alwaysRetainTaskState="true"
            android:screenOrientation="fullSensor"
            android:windowSoftInputMode="stateAlwaysHidden" />
        <activity android:name=".ToolActivity" />
        <activity android:name=".AllToolsActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>